version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: kanban-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kanban
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d kanban"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.backend
    container_name: kanban-flask-api
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/kanban
      PORT: 8080
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy

  kanban:
    build:
      context: ..
      dockerfile: docker/Dockerfile.frontend
    container_name: kanban-react-frontend
    ports:
      - "3000:3000"
    depends_on:
      - api

  proxy:
    image: nginx:alpine
    container_name: kanban-nginx-proxy
    ports:
      - "80:80"
    volumes:
      - ../nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
      - kanban

volumes:
  pgdata:
